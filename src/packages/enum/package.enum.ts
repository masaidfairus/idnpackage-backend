/**
 * Lokasi/status paket:
 * - SECURITY: di pos security
 * - DORM: di kantor asrama
 * - TAKEN: sudah diambil siswa
 */
export enum PackageLocation {
  SECURITY = 'security_post',
  DORM = 'dormitory_office',
  TAKEN = 'taken',
}
